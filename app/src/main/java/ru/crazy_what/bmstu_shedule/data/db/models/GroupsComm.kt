package ru.crazy_what.bmstu_shedule.data.db.models

import androidx.room.ColumnInfo
import androidx.room.Entity
import androidx.room.ForeignKey

// TODO настроить foreign keys ON DELETE CASCADE (?) и primary key
/*@Entity(tableName = "groups_comm", foreignKeys = [ForeignKey(entity =)], primaryKeys =)
data class GroupsComm(
    @ColumnInfo(name = "l_id")
    val lessId: Int,
    @ColumnInfo(name = "g_id")
    val groupId: Int,
)*/